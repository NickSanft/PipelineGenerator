{
  "name": "pipeline-gen",
  "version": "0.1.0",
  "description": "Analyze your repo. Generate your pipeline. Ship with confidence.",
  "type": "module",
  "bin": {
    "pipeline-gen": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli/index.ts",
    "test": "echo 'No CLI-specific tests yet — run pnpm test from workspace root'"
  },
  "dependencies": {
    "@pipeline-gen/core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "inquirer": "^9.3.7"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "@types/node": "^20.14.0",
    "tsx": "^4.15.7",
    "typescript": "^5.5.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
